server:
  port: 8001

spring:
  application:
    name: auth-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
  redis:
    host: localhost
    port: 6379
    database: 0

jwt:
  secret: bigdata_ecommerce_secret_key
  expiration: 86400
  refresh-expiration: 604800
